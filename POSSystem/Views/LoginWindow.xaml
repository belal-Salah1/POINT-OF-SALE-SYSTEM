<Window x:Class="POSSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="POS System - Login"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left brand panel -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                <TextBlock Text="🛒" FontSize="80" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="POS System" FontSize="42" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Point of Sale Management" FontSize="16" Foreground="#DBEAFE" HorizontalAlignment="Center" Margin="0,10,0,0"/>

                <Border Margin="0,40,0,0" Padding="20" Background="#1D4ED8" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Default Test Accounts" FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                        <TextBlock Text="Admin:    admin / admin123" FontSize="12" Foreground="#DBEAFE" FontFamily="Consolas"/>
                        <TextBlock Text="Cashier:  cashier1 / cashier123" FontSize="12" Foreground="#DBEAFE" FontFamily="Consolas"/>
                        <TextBlock Text="Cashier:  cashier2 / cashier123" FontSize="12" Foreground="#DBEAFE" FontFamily="Consolas"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Right login form -->
        <Grid Grid.Column="1" Background="White">
            <StackPanel VerticalAlignment="Center" Margin="60,0">
                <TextBlock Text="Welcome Back" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Sign in to continue to your dashboard" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,30"/>

                <TextBlock Text="Username" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBox x:Name="UsernameBox" Margin="0,0,0,16" KeyDown="OnEnterKey"/>

                <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,16" KeyDown="OnEnterKey"/>

                <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" FontSize="13" Margin="0,0,0,12" Visibility="Collapsed"/>

                <Button Content="Login" Style="{StaticResource PrimaryButton}" Height="44" Click="LoginButton_Click"/>

                <Grid Margin="0,20,0,0">
                    <TextBlock HorizontalAlignment="Center">
                        <Run Text="Don't have an account? " Foreground="{StaticResource TextSecondaryBrush}"/>
                        <Hyperlink Foreground="{StaticResource PrimaryBrush}" Click="SignupLink_Click" TextDecorations="None">
                            <Run Text="Sign up"/>
                        </Hyperlink>
                    </TextBlock>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
